# Listen on port
APP_PORT="3000"

# Base URL for the application
APP_BASE_URL="http://localhost:3000"

# CORS allowed origin  ( e.g. http://example.com,http://another.com, * = allow all)
# อนุญาตให้ตั้งค่า origin ได้หลายตัวคั่นด้วยคอมมา หรือใช้ "*" เพื่อทั้งหมด
CORS_ORIGIN="http://localhost:3000"

# Rate limit settings (requests per window per IP)
RATE_LIMIT_WINDOW_MS="60000" # ภายใน 1 นาที
RATE_LIMIT_MAX="100" # จำนวนครั้งสูงสุดที่อนุญาต

# Environment (development, production, test)
NODE_ENV="development"

# Log level ("fatal", "error", "warn", "info", "debug", "trace", "silent")
LOG_LEVEL="debug"

# Verbose log for test (1 = on, 0 = off)
TEST_VERBOSE_LOG="0"

# Force exit after timeout (ms)
FORCE_EXIT_MS="10000"

# Cache settings for redirect (in-memory)
REDIRECT_CACHE_TTL_MS="300000" # เก็บ cache เป็นเวลา 5 นาที
REDIRECT_CACHE_MAX="500" # เก็บสูงสุด 500 รายการ
REDIRECT_CACHE_SWEEP_MS="600000"  # ตรวจสอบและล้าง cache ที่หมดอายุทุก 10 นาที

# Database URL (Prisma)
# ตัวอย่างการตั้งค่า SQLite (ไฟล์จะถูกสร้างในโฟลเดอร์โปรเจค)
DATABASE_URL="file:./dev.db"